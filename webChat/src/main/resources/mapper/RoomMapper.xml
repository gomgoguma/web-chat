<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.webchat.room.RoomMapper">

    <select id="insertRoom" parameterType="int" resultType="int">
        INSERT INTO tb_room
        (
            create_dtm
          , host_id
        )
        VALUES
        (
            CURRENT_TIMESTAMP
          , #{userId}
        )
        RETURNING id
    </select>

    <insert id="insertRoomUser">
        INSERT INTO tb_room_user
        (
            user_id
          , room_id
          , join_dtm
        )
        VALUES
        <foreach collection="userIdList" item="item" separator="," >
        (
            #{item}
          , #{roomId}
          , CURRENT_TIMESTAMP
        )
        </foreach>
    </insert>

    <select id="getRooms" parameterType="int" resultType="com.webchat.room.object.RoomSearchResultObject">
        SELECT tr.*
             , (
                    SELECT STRING_AGG(u.name, ', ')
                    FROM tb_room_user ru
                    JOIN tb_user u ON u.id = ru.user_id
                    WHERE ru.room_id = tr.id
                      AND ru.user_id != tr.host_id
                ) room_name
        FROM tb_room tr
        JOIN tb_room_user tru ON tr.id = tru.room_id
        WHERE tru.user_id = #{id}
        ORDER BY id
    </select>

    <delete id="deleteRoomUser" parameterType="int">
        DELETE FROM tb_room_user
        WHERE room_id = #{id}
    </delete>

    <delete id="deleteRoom" parameterType="int">
        DELETE FROM tb_room
        WHERE id = #{id}
    </delete>

</mapper>